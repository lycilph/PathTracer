<UserControl x:Class="Studio.SceneScriptEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             xmlns:local="clr-namespace:Studio"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="110"/>
        </Grid.RowDefinitions>

        <!-- Code editor -->
        <avalonEdit:TextEditor x:Name="Editor"
                               Grid.Row="0"
                               ShowLineNumbers="True"
                               FontFamily="Consolas"
                               FontSize="13"
                               HorizontalScrollBarVisibility="Auto"
                               VerticalScrollBarVisibility="Auto"
                               WordWrap="False" />

        <!-- Diagnostics list -->
        <Border Grid.Row="1" BorderBrush="#444" BorderThickness="1" CornerRadius="4" Padding="6" Margin="0,8,0,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Script Diagnostics" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <ListBox ItemsSource="{Binding Diagnostics, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         SelectedItem="{Binding SelectedDiagnostic, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Display}" FontFamily="Consolas" TextWrapping="NoWrap"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button DockPanel.Dock="Bottom"
                        Content="Go to"
                        Margin="0,6,0,0"
                        Command="{Binding GoToDiagnosticCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding SelectedDiagnostic, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            </DockPanel>
        </Border>
    </Grid>

</UserControl>
